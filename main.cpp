#include "powing.h"

void test_powing()
{
    show_powing(2, 4);
    show_powing(5, 2);
    show_powing(3, 2);
    show_powing(4, 4);
    show_powing(5, 4);
}

int main()
{
    test_powing();
    return 0;
}